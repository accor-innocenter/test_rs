import { ContinueSessionMessage, EndSessionMessage } from 'hermes-javascript/types';
export default class Session {
    private mqtt;
    private sessionId;
    constructor();
    reset(): void;
    subscribe(topic: string): Promise<unknown>;
    unsubscribe(topic: string): Promise<unknown>;
    publishMessage({ intentName, input, ...additionalFields }: {
        [x: string]: any;
        intentName: any;
        input: any;
    }): Promise<unknown>;
    nextMessage(): Promise<[string, ContinueSessionMessage | EndSessionMessage]>;
    start({ intentName, input, ...additionalFields }: {
        [x: string]: any;
        intentName: any;
        input: any;
    }): Promise<void>;
    continue({ intentName, input, ...additionalFields }: {
        [x: string]: any;
        intentName: any;
        input: any;
    }): Promise<ContinueSessionMessage>;
    end(): Promise<EndSessionMessage>;
}
