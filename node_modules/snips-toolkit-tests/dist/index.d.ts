import { FetchMockSandbox } from 'fetch-mock';
import * as Tools from './tools';
import Session from './session';
declare global {
    type GenericObj = {
        [key: string]: any;
    };
    type HttpMockInitializer = (sandbox: FetchMockSandbox) => void;
    type ConfigMockInitializer = (config: GenericObj) => void;
    type GlobalsMockInitializer = (date: GenericObj) => void;
    const SnipsToolkit: {
        fetch: FetchMockSandbox;
        config: GenericObj;
        globals: GenericObj;
        mock: {
            http(initializer: HttpMockInitializer): void;
            config(initializer: ConfigMockInitializer): void;
            globals(initializer: GlobalsMockInitializer): void;
        };
    };
}
export declare const Test: {
    Session: typeof Session;
    Tools: typeof Tools;
};
